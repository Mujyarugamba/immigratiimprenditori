name: Completed one-shot Production ledger sync (inert)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary release runner is permanently inert."
