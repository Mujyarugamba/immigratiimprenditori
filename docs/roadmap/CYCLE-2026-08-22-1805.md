# Ciclo 2026-08-22 · 18:05 — DB locale, Fonti/Metodologia, Identità visiva

## Perimetro

Branch di ciclo: `feature/research-radar-ai-knowledge-20260822`

Base di integrazione: `feature/institutional-identity`.

Finestra operativa: 18:05–19:05 Europe/Rome, salvo stop anticipato per problema reale che richieda decisione congiunta.

Vincoli permanenti:

- nessun commit su `main`;
- nessuna migration applicata al database di produzione;
- nessun contatto inviato alle persone presenti nella short list interviste;
- nessuna pubblicazione automatica da Radar o AI;
- Netlify resta l'ambiente di preview; nessun deploy di produzione in questo ciclo.

## 1. Validazione database senza branch Supabase a pagamento

Stato: **PASS**

È operativo un workflow GitHub Actions che costruisce un database Supabase/Postgres locale effimero e usa esclusivamente la catena standalone Centro Studi:

1. baseline SPLIT-3 `supabase/baseline/00..03`;
2. fondazione editoriale standalone;
3. evoluzioni post-cutover realmente necessarie;
4. migration Centro Studi correnti del branch.

Verifiche concluse con successo:

- cold start completo da database vuoto;
- `supabase db lint --level error --fail-on error`;
- security smoke sul publication gate;
- smoke RLS/permessi della Inbox;
- distruzione dello stack locale al termine.

Conclusione: durante lo sviluppo il branch Supabase remoto a pagamento non è necessario. Un branch hosted potrà essere rivalutato soltanto nell'ultimo pre-rilascio se servirà un controllo di parità dell'infrastruttura ospitata.

Gate:

- `LOCAL_DB_COLD_START = PASS`
- `LOCAL_DB_LINT = PASS`
- `LOCAL_DB_SECURITY = PASS`
- `PAID_SUPABASE_BRANCH = NOT_NEEDED_NOW`
- `PRODUCTION_DB_UNTOUCHED = PASS`

## 2. Voci candidate / interviste

Stato: **SHORT LIST ONLY — NO OUTREACH**

La redazione dispone della pagina privata `Voci candidate` con short list e opzioni di formato. La modalità preferenziale è il video autonomo: il Centro Studi invia domande e istruzioni, la persona registra le risposte e invia il materiale; fact-check, autorizzazioni e decisione editoriale precedono sempre qualsiasi pubblicazione.

Nessun messaggio, invito o contatto è stato inviato.

Gate:

- `INTERVIEW_SHORTLIST = READY`
- `OUTREACH_SENT = 0`
- `SELF_RECORDED_VIDEO_WORKFLOW = READY`

## 3. Fonti e metodologia

Stato: **PASS**

La sezione pubblica `/fonti` mostra per ogni fonte statistica effettivamente collegata a valori pubblici:

- produttore e identificazione della fonte;
- indicatori del Centro Studi che la utilizzano;
- copertura geografica effettivamente pubblicata;
- periodicità degli indicatori collegati;
- periodo dei valori pubblicati;
- qualificazione dei valori;
- nota metodologica e limiti disponibili;
- licenza/condizioni di riuso quando registrate;
- collegamento alla fonte originale.

La metodologia generale separa esplicitamente:

- fonti ufficiali e istituzionali;
- ricerca accademica;
- istituti e centri di ricerca indipendenti;
- fonti associative e professionali;
- stampa e fonti giornalistiche;
- testimonianze dirette.

Il riepilogo automatico fonte → indicatori/copertura/periodi è coperto da test unitari. La CI applicativa ha verificato typecheck, test, build Next e HTTP smoke.

Gate:

- `SOURCES_METHODOLOGY = PASS`

## 4. Grafica e identità visiva

Stato: **QA IN CORSO**

La direzione istituzionale è già implementata in `src/app/editorial-identity.css`, importato dopo gli stili legacy. Lo strato corrente:

- porta la UI verso nero, bianco e grigi;
- elimina i gradienti visibili della Home;
- elimina ombre decorative e raggi da interfaccia startup;
- trasforma la fascia dati in una superficie chiara da rapporto/centro studi;
- mantiene un solo accento cromatico molto contenuto per funzioni/azioni;
- conserva immagini soltanto come materiale editoriale/documentario;
- mantiene tipografia serif per titoli e sans-serif per struttura e metadati.

Prima di promuovere `VISUAL_IDENTITY = PASS` resta da completare il QA delle eccezioni residue e l'allineamento del design system scritto.

## Verifica applicativa corrente

Ultima verifica completa disponibile nel ciclo:

- TypeScript: PASS;
- test: PASS;
- Next.js build: PASS;
- HTTP smoke: PASS;
- database locale cold-start/lint/security: PASS.

## Prossimi passi del ciclo

1. chiudere il design system editoriale e il QA `VISUAL_IDENTITY`;
2. verificare la Home v1 contro i sei moduli richiesti dalla roadmap;
3. aggiornare PR/checkpoint senza merge e senza deploy di produzione.
