# Ciclo 2026-08-22 · 18:05 — DB locale, Fonti/Metodologia, Identità visiva

## Perimetro

Branch di ciclo: `feature/research-radar-ai-knowledge-20260822`

Base di integrazione: `feature/institutional-identity`.

Finestra operativa: 18:05–19:05 Europe/Rome, salvo stop anticipato per problema reale che richieda decisione congiunta.

Vincoli permanenti:

- nessun commit su `main`;
- nessuna migration applicata al database di produzione;
- nessun contatto inviato alle persone presenti nella short list interviste;
- nessuna pubblicazione automatica da Radar o AI;
- Netlify resta l'ambiente di preview; nessun deploy di produzione in questo ciclo.

## 1. Validazione database senza branch Supabase a pagamento

Stato: **PASS**

È operativo un workflow GitHub Actions che costruisce un database Supabase/Postgres locale effimero e usa esclusivamente la catena standalone Centro Studi:

1. baseline SPLIT-3 `supabase/baseline/00..03`;
2. fondazione editoriale standalone;
3. evoluzioni post-cutover realmente necessarie;
4. migration Centro Studi correnti del branch.

Verifiche concluse con successo:

- cold start completo da database vuoto;
- `supabase db lint --level error --fail-on error`;
- security smoke sul publication gate;
- smoke RLS/permessi della Inbox;
- distruzione dello stack locale al termine.

Conclusione: durante lo sviluppo il branch Supabase remoto a pagamento non è necessario. Un branch hosted potrà essere rivalutato soltanto nell'ultimo pre-rilascio se servirà un controllo di parità dell'infrastruttura ospitata.

Gate:

- `LOCAL_DB_COLD_START = PASS`
- `LOCAL_DB_LINT = PASS`
- `LOCAL_DB_SECURITY = PASS`
- `PAID_SUPABASE_BRANCH = NOT_NEEDED_NOW`
- `PRODUCTION_DB_UNTOUCHED = PASS`

## 2. Voci candidate / interviste

Stato: **SHORT LIST ONLY — NO OUTREACH**

La redazione dispone della pagina privata `Voci candidate` con short list e opzioni di formato. La modalità preferenziale è il video autonomo: il Centro Studi invia domande e istruzioni, la persona registra le risposte e invia il materiale; fact-check, autorizzazioni e decisione editoriale precedono sempre qualsiasi pubblicazione.

Nessun messaggio, invito o contatto è stato inviato.

Gate:

- `INTERVIEW_SHORTLIST = READY`
- `OUTREACH_SENT = 0`
- `SELF_RECORDED_VIDEO_WORKFLOW = READY`

## 3. Fonti e metodologia

Stato: **PASS**

La sezione pubblica `/fonti` mostra per ogni fonte statistica effettivamente collegata a valori pubblici:

- produttore e identificazione della fonte;
- indicatori del Centro Studi che la utilizzano;
- copertura geografica effettivamente pubblicata;
- periodicità degli indicatori collegati;
- periodo dei valori pubblicati;
- qualificazione dei valori;
- nota metodologica e limiti disponibili;
- licenza/condizioni di riuso quando registrate;
- collegamento alla fonte originale.

La metodologia generale separa esplicitamente:

- fonti ufficiali e istituzionali;
- ricerca accademica;
- istituti e centri di ricerca indipendenti;
- fonti associative e professionali;
- stampa e fonti giornalistiche;
- testimonianze dirette.

Il riepilogo automatico fonte → indicatori/copertura/periodi è coperto da test unitari. La CI applicativa ha verificato typecheck, test, build Next e HTTP smoke.

Gate:

- `SOURCES_METHODOLOGY = PASS`

## 4. Grafica e identità visiva

Stato: **PASS**

Il design system canonico è documentato in `docs/roadmap/VISUAL-IDENTITY-V1.md` e implementato in `src/app/editorial-identity.css`, importato dopo gli stili legacy.

La direzione verificata:

- nero, bianco e grigi come linguaggio principale;
- gradienti visibili della Home neutralizzati;
- ombre decorative e raggi da interfaccia startup neutralizzati;
- fascia dati convertita in superficie chiara da rapporto/centro studi;
- un solo accento cromatico contenuto e funzionale;
- immagini ammesse come materiale editoriale/documentario, non come decorazione generica;
- serif per titoli/headline e sans-serif per struttura, metadati e controlli;
- header, Home, dati, Voci, Contribuisci e footer allineati allo stesso sistema.

Gate:

- `VISUAL_IDENTITY = PASS`

## 5. Homepage editoriale v1

Stato: **PASS**

La Home è documentata in `docs/roadmap/HOME-V1.md` e contiene i sei moduli richiesti dalla roadmap:

1. un dato importante tramite `I numeri chiave` dell'Osservatorio;
2. un rapporto/studio come feature principale, con preferenza per contenuti di ricerca;
3. una storia/intervista tramite `Le voci dell'imprenditoria migrante`;
4. un evento pubblico qualificato o fallback trasparente;
5. contenuti recenti/in evidenza senza duplicare le feature principali;
6. invito a contribuire con passaggio obbligatorio dalla Inbox e revisione redazionale.

I fallback non simulano dati, eventi, storie o contenuti inesistenti.

Gate:

- `HOME_V1 = PASS`

## 6. Sostegno economico

Stato: **PAGINA PRONTA / PAGAMENTI DISATTIVATI**

La pagina `/sostieni` spiega cosa viene finanziato — ricerca e dati, storie/interviste, rapporti/pubblicazioni e produzione audiovisiva — e separa esplicitamente sostegno economico e indipendenza editoriale.

La configurazione è fail-closed:

- `donationsOnlineEnabled = false`;
- provider non selezionato;
- payment URL assente;
- nessun pulsante di pagamento viene mostrato finché conto ricevente, intestazione, provider, dati amministrativi e formulazione fiscale non sono verificati.

Gate corrente:

- `SUPPORT_PAGE = PASS`
- `ONLINE_DONATIONS = INTENTIONALLY_DISABLED`
- `SUPPORT = PARTIAL / PAYMENT_GATE_DEFERRED`

## 7. SEO e architettura internazionale

Stato: **PASS — FOUNDATION**

La fondazione SEO internazionale è operativa e verificata:

- URL italiani canonici senza prefisso lingua;
- URL localizzati per EN, FR, ES, DE, AR e ZH;
- `hreflang` per le sette lingue + `x-default` italiano sulle route localizzate;
- sitemap statica e dinamica con contenuti, indicatori, eventi, Paesi, rotte, territori e settori solo quando sostenuti da evidenza pubblica;
- canonical delle pagine localizzate;
- le pagine che mostrano il contenuto originale non tradotto dentro un'interfaccia localizzata restano `noindex, follow` e canoniche verso l'originale italiano;
- Deploy Preview Netlify globalmente `noindex` tramite `robots.txt` fail-closed;
- produzione crawlable salvo `/app/` e `/accedi`;
- sitemap principale e sitemap contributor dichiarate in robots.

Test automatici coprono URL localizzati, set hreflang/x-default e comportamento robots preview/production.

Gate:

- `SEO_FOUNDATION = PASS`

## Verifica applicativa corrente

Ultima verifica completa disponibile nel ciclo:

- TypeScript: PASS;
- test: PASS;
- Next.js build: PASS;
- HTTP smoke: PASS;
- database locale cold-start/lint/security: PASS;
- Netlify Deploy Preview #9: PASS;
- nessun deploy di produzione;
- `main` non toccato.

Gli status Vercel legacy non fanno parte del workflow corrente e non sono gate del ciclo.

## Prossimi passi del ciclo

1. aggiornare Production Readiness per sostituire il vecchio blocco `DB_NONPROD = NOT_AVAILABLE` con la validazione locale realmente completata;
2. aggiornare l'Execution Master con i gate Fonti/Metodologia, Identità visiva, Home e SEO ora chiusi;
3. mantenere pagamenti e profili social esterni disattivati finché non sono realmente verificati;
4. avanzare sui quality gate gratuiti senza merge e senza deploy di produzione.
