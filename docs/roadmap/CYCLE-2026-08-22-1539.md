# Ciclo 2026-08-22 · 15:39 — Ricerca, Radar, AI, Knowledge Graph

## Perimetro

Branch di ciclo: `feature/research-radar-ai-knowledge-20260822`

Base: `feature/institutional-identity` @ `b0810a3f614912ecf53eeee4c356a8177a45d185`.

Vincoli del ciclo:

- nessun commit su `main`;
- nessuna migration applicata al database di produzione;
- nessuna pubblicazione automatica da Radar o AI;
- le funzioni nuove devono degradare in modo sicuro se le migration preparate non sono ancora presenti nell'ambiente collegato.

## 1. Ricerca avanzata

Stato: **IMPLEMENTED ON BRANCH**

- Ricerca pubblica unificata su contenuti, indicatori ed eventi già disponibili.
- Integrazione opzionale dell'indice `search_documents` per Paesi, territori, settori, rotte, autori e fonti.
- Full-text search `websearch` sull'indice preparato quando disponibile.
- Fallback automatico sul modello dati già attivo se `search_documents` non è ancora presente.
- Filtri pubblici per tipo di entità e anno.
- Ordinamento per pertinenza, poi data.

Nota: la migration `20260822155000_knowledge_search_architecture.sql` resta soltanto preparata; non è stata applicata alla produzione in questo ciclo.

## 2. Radar editoriale

Stato: **IMPLEMENTED ON BRANCH**

- Dashboard privata `/app/redazione/radar` collegata alla Inbox esistente.
- Contatori per nuovi, da valutare e da approfondire.
- Vista per fonte e banda geografica.
- Radar notturno aggiornato a v1.1:
  - normalizzazione URL e rimozione parametri di tracking;
  - scoring deterministico di rilevanza 0–100;
  - priorità `high`, `normal`, `low` derivata dallo score;
  - classificazione documentale coerente con i tipi Inbox;
  - deduplicazione esatta per URL;
  - deduplicazione recente per fingerprint titolo + fonte;
  - ordinamento dei candidati per rilevanza prima del limite di inserimento;
  - metadati di audit (`radar_relevance_score`, `radar_title_fingerprint`, `radar_version`).
- `auto_publish` resta sempre `false`.

## 3. AI redazionale

Stato: **WORKBENCH IMPLEMENTED; DB ACTIVATION INTENTIONALLY DEFERRED**

- Nuova area privata `/app/redazione/ai`.
- Registro dei run con task, provider, modello, versione prompt, stato e timestamp.
- Revisione umana con stati `reviewed`, `accepted`, `rejected`.
- Un run fallito non può essere approvato.
- Se la tabella `editorial_ai_runs` non è disponibile, la pagina mostra esplicitamente che l'architettura è preparata ma non attivata.
- Nessun provider/modello viene imposto dal ciclo e nessun output AI viene pubblicato automaticamente.

Nota: la migration `20260822161000_editorial_automation_architecture.sql` resta soltanto preparata; non è stata applicata alla produzione in questo ciclo.

## 4. Knowledge Graph e pagine relazionali

Stato: **IMPLEMENTED ON BRANCH**

- Il grafo pubblico continua a essere derivato soltanto da dati pubblicati e verificabili.
- Entità v1: Paesi, indicatori, settori, rotte.
- Aggiunta funzione di vicinato con connessioni incoming/outgoing.
- Nuove pagine `/relazioni/[kind]/[key]` per attraversare le relazioni documentate.
- `/relazioni` collega ora alle pagine relazionali dedicate.
- API `/api/v1/graph`:
  - snapshot completo senza parametri;
  - vicinato con `?kind=...&key=...`;
  - 400 su parametri incompleti;
  - 404 su entità pubblica inesistente.
- Nessuna relazione AI-inferita viene esposta come fatto.

## Verifica

- Il branch è separato da `main` ed è basato sul lavoro più avanzato di `feature/institutional-identity`.
- Il workflow CI è stato esteso al solo branch di ciclo per eseguire `npm ci`, `npm run typecheck` e `npm test` anche senza aprire una PR verso `main`.
- È stato aggiunto un test unitario per la costruzione degli URL relazionali.
- Gli status Vercel osservabili sul branch risultano bloccati dal build-rate-limit del progetto Vercel; questo non è un esito di typecheck/test del codice.
- Con gli strumenti disponibili in questa sessione non è stato possibile leggere un risultato conclusivo del workflow push di GitHub Actions, quindi non viene dichiarato un PASS CI non verificato.

## Gate del ciclo

- `ADVANCED_SEARCH = BRANCH_READY`
- `RADAR_EDITORIAL = BRANCH_READY`
- `EDITORIAL_AI_REVIEW = BRANCH_READY / DB_NOT_ACTIVATED`
- `KNOWLEDGE_GRAPH_RELATIONAL = BRANCH_READY`
- `MAIN_UNTOUCHED = PASS`
- `PRODUCTION_DB_UNTOUCHED = PASS`

Nessun gate di produzione viene promosso da questo documento: il ciclo prepara e verifica il lavoro sul branch, mantenendo separato l'atto successivo di applicazione migration, configurazione provider AI, preview finale e promozione verso produzione.
