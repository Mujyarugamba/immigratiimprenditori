# P4.5 — Ecosystem UX & Information Architecture Validation Report

**Date:** 2026-08-07
**Branch:** `main` (uncommitted; no commit/push)
**DB head:** `20260812300000` local = remote, pending **0**
**DB/RLS/Access:** **unchanged**
**Decision:** P4.5 ECOSYSTEM UX & INFORMATION ARCHITECTURE COMPLETATO — P5 EDITORIAL/ADMIN AUTORIZZABILE

---

## 1. Identità pubblica

**“Persone. Imprese. Opportunità. Mercati internazionali.”**

Value proposition: rete economica digitale dove i fatti esistono una volta e si leggono da più contesti.
L’Osservatorio **non** è l’identità della piattaforma.

Source of truth in code: `src/data/ecosystems.ts`.

## 2. Cinque ecosistemi

| Ecosistema | Hub / route | Note |
|---|---|---|
| Persone | `/persone` | Hub + professionisti pubblici; no directory Persona completa in v1 |
| Imprese | `/imprese` | Nodo principale + cross-link |
| Opportunità e collaborazioni | `/opportunita` + `/collaborazioni` | UX unitaria, AR distinti |
| Mercati internazionali | `/mercati` | Ingresso relazionale, non solo elenco |
| Servizi | `/servizi` | Offerta ≠ Richiesta |

## 3. Livelli trasversali

Eventi, Notizie/Guide (`/contenuti`), Osservatorio, Organizzazioni — in nav **Esplora** e strip home, senza dominare l’identità.

## 4. Mapping DB → UX

Documentato in `dbDomainToEcosystem` (`ecosystems.ts`): es. `collaborations` → ecosistema opportunità; `events`/`contents`/`observatory_*` → trasversale.

## 5. Home

- Hero identità + value prop
- Griglia cinque ecosistemi
- Sezioni dati reali per ecosistema (empty se DB vuoto)
- Strip trasversale
- CTA verso registrazione / workspace / pubblica
- Nessuna statistica inventata; Osservatorio non in evidenza brand

## 6. Navigation

**Primary:** Persone, Imprese, Opportunità, Mercati, Servizi
**Esplora:** Collaborazioni, Professionisti, Eventi, Notizie e guide, Osservatorio, Organizzazioni, Chi siamo

## 7. Route P4

Conservate (`/imprese`, `/professionisti`, `/opportunita`, `/servizi`, `/eventi`, `/collaborazioni`, `/mercati`, `/organizzazioni`, `/osservatorio`, `/contenuti` + detail).
Aggiunta hub `/persone` (non sostituisce directory mancante).

## 8. Cross-link

Modulo `src/lib/data/public/related.ts` + `RelatedLinks` su detail:
impresa, professionista, mercato, opportunità, evento, offerta servizio, contenuto.
Solo SELECT pubbliche (RLS); batch `Promise.all`; fail-soft `.catch(() => [])`.

## 9. CTA

`/pubblica` ridefinita come mappa di azioni reali (registrati, `/app/imprese`, elenchi pubblici).
FinalCta home allineato. Nessuna fake feature.

## 10. Limiti v1

- Nessuna directory pubblica completa Persone (`profiles.is_public` esiste, ma prodotto v1 usa professionisti + hub)
- Territori/lingue/settori come filtri/dimensioni dove già presenti, non cataloghi UX nuovi
- Verifica/reputazione: non implementata
- Markdown contenuti: pre-wrap (P4)

## 11. P5 / P6

**P5:** CRUD Red/Osservatorio/Org, console Adm, pubblicazione editoriale.
**P6:** redesign visuale pieno, SEO/a11y avanzate, hardening E2E.

## 12. Gates (chiusura)

| Gate | Result |
|---|---|
| `npm test` | **75 pass / 0 fail** |
| `npm run typecheck` | 0 |
| `npm run lint` | 0 |
| `npm run build` | 0 |
| `npm run test:p4-smoke` | **P4_SMOKE_PASS** / cleanup done |
| `npm run test:p3-smoke` | **P3_SMOKE_PASS** / cleanup done |
| DB locale/remoto | `20260812300000` |
| pending | **0** |
| migration create | **nessuna** |
| RLS | **invariata** |

## 13. Decisione

**P4.5 ECOSYSTEM UX & INFORMATION ARCHITECTURE COMPLETATO — P5 EDITORIAL/ADMIN AUTORIZZABILE**
