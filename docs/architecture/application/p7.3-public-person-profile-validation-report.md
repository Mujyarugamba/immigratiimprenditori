# P7.3 — Public Person Profile Validation Report

**Date:** 2026-08-09
**Status:** PASS (app-only)
**Baseline Git:** `949eead76b03f90dd58d1cbd288fb0388afaf544`
**DB head (local = remote):** `20260813150000` · pending `0`
**Migrations created:** `0`
**Schema/RLS:** unchanged

---

## 1. Decision

**P7.3 PUBLIC PERSON PROFILE COMPLETATO — `/persone/[slug]` OPERATIVO E PRIVACY-SAFE — P7.4 FRONTEND HUMANIZATION AUTORIZZABILE**

---

## 2. Visibility criterion

Authoritative RLS (`profiles_select_public`):

`is_public = true AND is_active = true AND deleted_at IS NULL`

App defense-in-depth: `getPublicPersonBySlug` also filters `.eq("is_public", true).eq("is_active", true)` so an authenticated owner cannot open a private profile via the public route.

Missing / private / unreadable → `null` → `notFound()` (no existence leak).

---

## 3. Public fields

Selected: `id, slug, display_name, bio, city, province, region, country, website, avatar_url, organization_name, role_description`

Excluded from select/UI: `phone`, email/Auth, account ids, lifecycle/audit, grants, CTX/ACT.

---

## 4. Related sections

| Section | Source | Notes |
|---|---|---|
| Professional | `professional_profiles` by `person_id` | RLS published+public; link to `/professionisti/[id]` |
| Businesses | `business_memberships` + `businesses` | Public membership visibility only; no CTX/ACT |
| Cultura | via cultural_creative categories | Cross-link `/cultura` when applicable |

No slug history / redirects for changed addresses (P7.2 note remains).

---

## 5. Dirt excluded

- `.gitignore`
- `docs/architecture/application/application-v1-deployment-report.md`

---

## 6. Gates

| Gate | Result |
|---|---|
| `npm test` | **133 pass / 0 fail** |
| typecheck / lint / build | 0 — route `ƒ /persone/[slug]` |
| E2E people + public | **16 pass / 0 fail** |
| Migration create | 0 |
| DB head | `20260813150000` |
