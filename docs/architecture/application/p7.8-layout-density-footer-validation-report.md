# P7.8 â€” Layout density & footer compactness â€” validation report

**Date:** 2026-08-13
**Branch:** `main`
**Baseline HEAD:** `5ecac16`
**Scope:** presentation / layout / CSS only â€” no routes, copy (except footer group labels), DB, auth, legal logic, D1, data fetching.

---

## 1. Esito

**P7.8 LAYOUT DENSITY & FOOTER COMPACTNESS COMPLETATO**
Home empty sections are naturally short; empty-state boxes are compact; footer is a 5-column horizontal grouping on desktop. Semantics / functionality / DB / auth unchanged. Production redeploy authorizable (deploy not executed).

## 2. Baseline

| Item | Value |
|------|--------|
| Branch | `main` |
| HEAD / origin/main | `5ecac16` (0/0) |
| Pre-existing dirt preserved | `.gitignore`, `legal-drafting-factual-brief.md`, `application-v1-deployment-report.md`, `l1.2-legal-review-report.md` |

## 3. Componenti ispezionati

`Section`, `EmptyState`, `PublicEmpty`, `HomeDomainSection`, `HomeSectionHeader`, `Hero`, `EcosystemGrid`, `TransversalStrip`, `FinalCta`, `Footer`, `page.tsx` (Home), `navigation.ts` (data only, unread for IA).

## 4. Cause spazi vuoti

1. `HomeDomainSection` used `py-14 sm:py-16 lg:py-20` stacked on `Section` default `py-8 sm:py-10`.
2. `HomeSectionHeader` large bottom margin (`mb-8 sm:mb-10`) + `items-end` pushed CTA far down.
3. `EmptyState` used `mt-8` + `py-8` (tall dashed box for one line).
4. Footer used one tall â€œNavigazioneâ€ column (`mainNav` â‰ˆ 13 links) beside short Partecipa/Contatti.

## 5. Section spacing pre

Home domain: ~`py-14`â€“`py-20` effective intent; header margin `mb-8`/`mb-10`; title scale up to `lg:text-[2.5rem]`.

## 6. Section spacing post

| State | Padding | Header |
|-------|---------|--------|
| Empty | `py-6 sm:py-7` | `compact` (smaller type, `mb-4`) |
| Populated | `py-8 sm:py-10` | default denser (`mb-5 sm:mb-6`, `text-2xl/3xl`) |

`Section` skips default `py-*` when caller already passes `py-*`. Measured empty domain section â‰ˆ **202â€“226px** (was visually card-grid tall).

## 7. Empty-state pre

`border-dashed` + `mt-8` + `px-5 py-8` + `h2` â€” large block.

## 8. Empty-state post

`px-4 py-3`, no top margin, `text-sm` paragraph. Measured box height â‰ˆ **46px** (`padding` 12px/12px).

## 9. Hero

Micro-only: `py-8 sm:py-11`, `space-y-4`. Headline / search / CTAs / palette unchanged.

## 10. Five doors

Micro-only: `py-10 sm:py-12`, header `mb-6`. Five cards remain; not aggressively compressed.

## 11â€“16. Domain sections

Persone / Imprese / OpportunitÃ  / Mercati / Servizi / Eventi: shared `HomeDomainSection` density-aware padding + compact empty header + short empty box. CTA in `1fr / auto` grid, top-aligned (`sm:pt-6` under eyebrow).

## 17. Altri percorsi

Unchanged spacing philosophy (`py-10 sm:py-12`) â€” used as density reference.

## 18. Final CTA

Slightly tighter: `py-10 sm:py-12`, inner `py-7`/`py-8`, heading `text-2xl/3xl`. Copy unchanged.

## 19. Footer pre

4 columns; single Navigazione list of all `mainNav` links â†’ excess height / dead space under Contatti.

## 20. Footer post

Desktop `lg:grid-cols-5`; tablet `md:grid-cols-3` / `sm:grid-cols-2`; mobile stack. Padding `py-6`/`lg:py-7`; legal row `py-3`. Measured footer â‰ˆ **341px**.

## 21. Footer grouping

| Column | Source |
|--------|--------|
| Brand | site identity + description |
| Esplora | `primaryNav` |
| Rete | `moreNav` ordered (Professionisti â†’ Chi siamo) |
| Partecipa | profile / impresa / pubblica |
| Contatti | email + Scrivici |
| Bottom | Â© + Privacy / Cookie / Termini / Dati e fonti |

No domains dropped; no accordion / internal scroll.

## 22. Desktop

1440: 5 footer columns; empty sections short; five doors readable. Visual QA via local screenshots + CDP metrics.

## 23. Tablet

`sm:grid-cols-2` / `md:grid-cols-3` â€” groups preserved, no overflow intended.

## 24. Mobile

Footer stacks; section CTAs stack under copy; empty boxes stay short.

## 25. CSS/tokens changed

No new design-token file. Coherent utility scale: empty `py-6/7`, default `py-8/10`, hero/five-doors slightly reduced.

## 26. Shared components changed

`Section`, `EmptyState`, `HomeDomainSection`, `HomeSectionHeader`, `Footer`, light touches to `Hero` / `EcosystemGrid` / `FinalCta`, Home `page.tsx` classNames, copy tests for footer imports.

## 27. Routes changed = NO

## 28. DB changed = NO

## 29. Migration changed = NO

## 30. Authorization changed = NO

## 31. Legal changed = NO

## 32. Data fetching changed = NO

## 33. npm test

**PASS** â€” 190/190.

## 34. typecheck

**PASS**

## 35. lint

**PASS** (0 errors; pre-existing unused import warning in terms test).

## 36. build

**PASS**

## 37. E2E

| Suite | Result |
|-------|--------|
| ia | PASS |
| legal (incl. footer legal links) | PASS |
| public loads | PASS |
| public not-found / unknown slug | FAIL â€” page stuck on loading shell (â€œCaricamento notizie e guideâ€¦â€) under Playwright webServer; **not** caused by density CSS (ErrorState/`not-found` unchanged) |

## 38. visual review

Local CDP: empty box 46px; empty sections ~202px; page scroll height ~2955px; footer 5-col ~341px. Screenshots: hero/five-doors, empty sections, Altri percorsi + Eventi empty + Final CTA + footer.

## 39. Files

- `src/components/ui/Section.tsx`
- `src/components/ui/EmptyState.tsx`
- `src/components/home/HomeDomainSection.tsx`
- `src/components/home/HomeSectionHeader.tsx`
- `src/components/home/Hero.tsx`
- `src/components/home/EcosystemGrid.tsx`
- `src/components/home/FinalCta.tsx`
- `src/components/layout/Footer.tsx`
- `src/app/page.tsx`
- `src/lib/public/copy-humanization.test.ts`
- `docs/architecture/application/p7.8-layout-density-footer-validation-report.md`

## 40. Dirt preserved

Yes â€” only P7.8 paths staged.

## 41. git diff check

Run on staged set at commit time.

## 42. Staging

Selective paths only (no `git add .` / `-A`).

## 43. Commit

`fix(app): tighten layout density and footer`

## 44. Hash

Filled after commit.

## 45. Push

`origin/main`

## 46. HEAD/origin

Filled after push.

## 47. Blocking findings

None for layout scope.

## 48. Non-blocking findings

- Playwright not-found E2E flaky/loading under local webServer.
- Pre-existing eslint unused import in terms acceptance test.

## 49. Deploy readiness

**Production redeploy authorizable** after push. Deploy not executed.

## 50. Decisione finale

**P7.8 LAYOUT DENSITY & FOOTER COMPACTNESS COMPLETATO â€” HOME VISIVAMENTE PIÃ™ COMPATTA â€” EMPTY STATES RIDOTTI â€” FOOTER RIDISTRIBUITO ORIZZONTALMENTE â€” RESPONSIVE PRESERVATO â€” SEMANTICA/FUNZIONALITÃ€/DB/AUTH INVARIATI â€” PRODUCTION REDEPLOY AUTORIZZABILE**

STOP. NON DEPLOY. NON RIPRENDERE D1.
