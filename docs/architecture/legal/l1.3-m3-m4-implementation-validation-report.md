# L1.3-M3+M4 — Implementation validation report

**Units:** M3 self-service account deletion · M4 management reassignment  
**Date:** 2026-08-12  
**Migrations:**  
- `supabase/migrations/20260817100000_create_self_service_account_deletion.sql`  
- `supabase/migrations/20260818100000_create_management_reassignment_cases.sql`  
**Baseline Git (pre):** `0de69d0` (= origin/main, M2 closed)  
**Baseline DB (pre):** `20260816100000` local = remote · pending M3 then M4 only

---

## Decision

**L1.3 M3+M4 COMPLETATO — SELF-SERVICE ACCOUNT DELETION OPERATIVA — ORPHAN REASSIGNMENT OPERATIVO — AUTH/PRIVACY/RETENTION ALLINEATI — MIGRATION M3/M4 APPLICATE LOCALE/REMOTO — VERSIONATO SU ORIGIN/MAIN — LEGAL FINALIZATION AUTORIZZABILE**

*(Subject to final git push and Production HMAC config note below.)*

---

## Local apply

| Step | Result |
|---|---|
| Docker / Supabase local | Available |
| Apply order | **M3 → M4** only |
| Local head post | `20260818100000` |

## Runtime harness

`scripts/m3-m4-cumulative-runtime-check.mjs` — **PASS** (+ fixture cleanup)

Coverage exercised: simple account close; public profile unpublish; multi-manager business; last-manager → pending case; org owner → pending case; last admin refuse; terms soft-close preserved; no auto M2; spoof/anon deny; retry idempotent.

## Auth ban + UX

| Item | Result |
|---|---|
| Ban helper | `src/lib/access/ban-auth-user.ts` via `createAdminClient` (server-only) |
| Order | preflight → DB RPC → Auth ban → signOut |
| Ban fail after DB | account stays `closed`; controlled error + retry path; no reopen |
| UX | `/app/profilo` · password + type `CANCELLA` · Privacy `/privacy` |
| Post-success | redirect `/?account_deleted=1` |

## subject_ref / HMAC

| Item | Result |
|---|---|
| Format | `ls_` + HMAC-SHA256(account reference) |
| Secret | `LEGAL_SUBJECT_HMAC_SECRET` (env; not in repo) |
| Generation | Only when creating a concrete M2 record — **not** on every self-delete |
| M3 default | **zero** M2 inserts |

## Admin reassignment UI

| Item | Result |
|---|---|
| Route | `/app/amministrazione/riassegnazioni` (Adm only) |
| Resolve | Canonical M4 RPCs (business ACT bootstrap / org `owner_person_id`) |
| Auto-transfer | **No** |

## App gates

| Gate | Result |
|---|---|
| npm test | PASS (180) |
| typecheck | PASS |
| lint | PASS (1 pre-existing warning) |
| build | PASS |
| E2E `e2e/self-delete.spec.ts` | **PASS** (3/3 chromium; used existing `ms-playwright` browsers) |

## Remote

| Gate | Result |
|---|---|
| Dry-run pending | Exactly M3 then M4 |
| Apply | M3 → M4 |
| Head local = remote | `20260818100000` |
| Pending | 0 |
| Real remote deletes | **Not executed** |

## Production secret

| Item | Class |
|---|---|
| `LEGAL_SUBJECT_HMAC_SECRET` on Vercel Production | **MANUAL DEPLOYMENT CONFIG REQUIRED** — not inventable in code; confirm before app deploy |
| Vercel deploy this session | **NOT executed** |

## Legal alignment (Claude texts)

| Requirement | Status |
|---|---|
| Self-delete available | Implemented |
| Autonomous entities preserved | Implemented |
| Archive only for concrete reason | Implemented (no auto M2) |
| No auto-delete aggregates | Implemented |
| Account termination real (soft-close + ban + wipe) | Implemented |
| Public pages free of internal markers | Preserved (no new legal draft publish) |

## Out of scope (STOP)

- M5 / further migrations  
- Vercel deploy  
- M1/M2 changes  

---

*End M3+M4 implementation validation report*
