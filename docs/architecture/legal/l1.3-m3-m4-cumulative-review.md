# L1.3 — Cumulative review M3 + M4 (self-delete + reassignment)

**Date:** 2026-08-12  
**Migrations (applied local+remote, order M3→M4):**  
- `20260817100000_create_self_service_account_deletion.sql`  
- `20260818100000_create_management_reassignment_cases.sql`  
**Decision:** **APPLIED** — see `l1.3-m3-m4-implementation-validation-report.md`

---

## End-to-end flow (authorized)

1. User opens Cancella account → password re-auth + type `CANCELLA` + `/privacy` link  
2. Server: `access_self_delete_preflight()` (identity from session only)  
3. Hard block only: `last_application_admin`  
4. Informational: `m4_cases_will_open` / orphan ids  
5. Advisory lock on account  
6. For sole ACT businesses: `access_m4_open_business_reassignment(..., allow_while_granted=true)`  
7. For org `owner_person_id`: `access_m4_open_organization_reassignment` (owner FK kept)  
8. Revoke roles; revoke BMMA (trigger ensures pending if zero ACT); conclude memberships  
9. Delete contact channels; unpublish professional; anonymize officials  
10. Minimize Persona; soft-close Account  
11. M2 archive **only if** concrete reason (not automatic)  
12. Server Auth **ban**  
13. Client logout / redirect  
14. Aggregates remain; admin later resolves M4 cases via canonical ACT / owner_person_id  

## Legal consistency (Privacy §11.1 A/B/C · Termini §16.1)

| Requirement | Status |
|---|---|
| A presence removed | Persona soft-delete / unpublish |
| B ops PII removed | channels deleted; fields wiped |
| C minimized residual | M2 optional only; M4 is ops workflow not legal archive |
| No full profile archive | Pass |
| Autonomous entities kept | Pass |
| No auto-owner | Pass |

## M3 modifications in this phase

| Change | Why |
|---|---|
| Standalone M3 keeps refuse for orphans | Safe if M3 applied alone |
| M4 replaces `access_self_delete_*` | Orphans open cases; user not forced to find successor |
| Preflight keys `orphan_*`, `m4_cases_will_open` | UX/admin transparency without blocking |

## Apply order (executed)

1. Local apply **M3** then **M4**  
2. Runtime harness fixtures + cleanup  
3. Auth ban + UX + admin reassignment surface  
4. Remote dry-run → remote apply same order  
5. Commit/push M3+M4 block only · **no Vercel deploy**

**Manual residual:** confirm `LEGAL_SUBJECT_HMAC_SECRET` on Vercel Production before app deploy.

---

*End cumulative review*
