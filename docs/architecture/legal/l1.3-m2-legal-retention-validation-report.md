# L1.3-M2 — Legal retention archive validation report

**Unit:** M2 minimized legal retention archive  
**Date:** 2026-08-12  
**Migration:** `supabase/migrations/20260816100000_create_legal_retention_records.sql`  
**Baseline Git (pre):** `ef02ba0` (= origin/main, M1 closed)

## Decision

**L1.3-M2 LEGAL RETENTION ARCHIVE COMPLETATA — MIGRATION APPLICATA LOCALE/REMOTO — RUNTIME ACCESS/MINIMIZATION/RETENTION PASS — PARITÀ DB CONFERMATA**

## Local apply

- Applied **only** `20260816100000` via `supabase migration up --include-all`
- No other pending migrations applied

## Schema (local + remote)

Table `public.legal_retention_records` with typed minimized columns; FK `source_account_id → accounts(id) ON DELETE SET NULL`; closed reason/kind/class CHECKs; retention determinable; dispose gate; RLS admin SELECT; no authenticated INSERT/UPDATE/DELETE; helpers:

- `legal_retention_insert_record` — SECURITY DEFINER, `search_path=''`, EXECUTE `service_role` only
- `legal_retention_dispose_record` — same

## Runtime harness

`scripts/m2-legal-retention-runtime-check.mjs` — **ALL PASS**

| Area | Result |
|---|---|
| Anon CRUD | deny |
| Authenticated normal | deny (no row visibility) |
| Editor | deny |
| Admin | SELECT pass; direct write deny |
| Service insert / dispose | pass |
| Invalid reason / retention / proof | fail as designed |
| Dispose clears proof | pass |
| Dispose idempotent | pass |
| M1 terms RESTRICT preserved | pass |
| Fixture cleanup / count restore | pass |

## App gates

| Gate | Result |
|---|---|
| npm test | PASS (170) |
| typecheck | PASS |
| lint | PASS (1 pre-existing unused-import warning in M1 test) |
| build | PASS |

## Remote

| Gate | Result |
|---|---|
| Dry-run | **only** M2 |
| Apply | PASS |
| Head local = remote | `20260816100000` |
| Pending | 0 |

## Out of scope (STOP)

M3 self-delete · subject_ref hashing · M4 orphan · frontend archive UI · real remote archive inserts
