# D1.2 â€” Ingestion validation report

**Data verifica:** 2026-08-13
**Branch:** `main`
**HEAD:** `9873542653ba7a92e38656725078d4dabc1c1663`
**ModalitÃ :** design + dry-run scaffolding â€” **no apply, no Production import, no commit/push in this pass**

---

## 1. Esito

**D1.2 DATA MAPPING & INGESTION CONTRACT COMPLETATO â€” FONTI/LICENZE/PROVENANCE VERIFICATE â€” PILOTA OSSERVATORIO ITALIA+LOMBARDIA DEFINITO â€” IMPORTER/DRY-RUN PRONTI â€” NESSUNA MIGRATION NECESSARIA â€” PRIMO IMPORT REALE AUTORIZZABILE (D1.3A)**

## 2. Baseline Git

| Item | Value |
|---|---|
| Branch | `main` |
| HEAD | `9873542` |
| origin/main | `9873542` |
| Ahead/behind | `0 0` |
| Dirt preserved | `.gitignore`, `legal-drafting-factual-brief.md`, `application-v1-deployment-report.md`, `l1.2-legal-review-report.md` |

## 3. Baseline DB

| Item | Value |
|---|---|
| Local migrations head (filesystem) | `20260818100000` |
| Remote migration list (CLI) | **Unavailable this session** â€” `LegacyDbConfigConnectTempRoleError` / password auth failed |
| Pending known from prior D1.2 explore | 0 when last successful list |
| D1.2 SQL apply | **NO** |

## 4. D1.1 state

**COMPLETATA.** Registry, domain mapping, discovery report, roadmap, source-notes presenti sotto `docs/architecture/external-data/`.

## 5. Legal readiness

Privacy / Termini / Informativa Â«Dati e fonti esterneÂ» (v 2026-08-12) operative; pagina `/dati-e-fonti` attiva. Allineata a: provenance, no scraping nominativo, fonte primaria prevale.

## 6. Sources reverified

| Source | Check 2026-08-13 | Status |
|---|---|---|
| Eurostat `lfsa_esgan` API | Live JSON IT/FOR/2023 â†’ **287.7** THS_PER; updated 2026-06-30 | OK |
| Eurostat reuse policy | Free re-use + attribution (Commission reuse / Eurostat copyright notice) | REUSABLE_WITH_ATTRIBUTION |
| Unioncamere OpenGov / Futurae | Conferma contratto D1.2; CSV per-CCIAA; def. nascita | OK design; allowlist CSV ancora da chiudere in D1.3 |
| ISTAT / OECD / WB / incentivi.gov | Mapping wave successive invariato | Deferred |

## 7. Source registry DB status

**Documentale + seed runtime su `observatory_statistical_sources`.** Nessuna tabella `external_data_sources` / `external_ingestion_runs`. Sufficiente con P-D.

## 8. Provenance current model

Sources + mandatory `source_id` on values + methodology notes + supersedes. Opportunity: `opportunity_sources.consulted_at` proxy.

## 9. Provenance gaps

| Gap | Pilot mitigation |
|---|---|
| `retrieved_at` | Sidecar run manifest |
| observation external id | Natural key string |
| `license_code` tipizzato | `license_note` free text |
| ingestion_run table | `artifacts/ingestion/<run_id>/` (D1.3) |

## 10. License model

OPEN_REUSABLE / REUSABLE_WITH_ATTRIBUTION / API_TERMS / CURATED_ONLY / UNCLEAR / DO_NOT_INGEST.
Eurostat pilot: **REUSABLE_WITH_ATTRIBUTION**. UNCLEAR â†’ no ingest.

## 11. Ingestion-run model

Sidecar JSON (P-D): run_id, retrieved_at, counts, per-record natural_key + checksum + action. No DB table in D1.2/D1.3A.

## 12. Observatory schema status

**Sufficiente (D1.2-A).** Tables: indicators, statistical_sources, indicator_values (+ RLS).

## 13. Opportunity schema status

Sufficient for D1-B **after** Osservatorio E2E: origin/editorial/publication axes + `opportunity_sources`. Review obbligatoria. No import in D1.2.

## 14. Market schema status

**M1 only** â€” `international_market_support_resources`. No numeric indicator store. WB mapping deferred.

## 15. Culture/event/content status

CURATION / SEMI-AUTO + review; no dump; no full-text copy. Deferred.

## 16. Pilot geography

Italia + Lombardia (pilota tecnico; prodotto nazionale).

## 17. Indicator budget

Wave A **11** (â‰¤12). Pilot **4**.

## 18. Selected P0 indicators

`OBS-UC-STR-STOCK`, `OBS-UC-STR-SHARE`, `OBS-UC-STR-YOY`, `OBS-EU-SELF-CIT`.

## 19. Selected P1 indicators

`OBS-UC-STR-SECTOR`, `OBS-EU-SELF-CIT-SHARE`, `OBS-IST-FOR-STOCK`.

## 20. Definitions

Non fusione nascita / citizenship / impresa straniera / self-employed. Vedi pilot mapping Â§1.

## 21. Unioncamere mapping

Futurae def. + OpenGov CSV allowlist + SEMI national extract. Keys `OBS-UC-*|{terr}|{YYYY}`.

## 22. Eurostat mapping

`lfsa_esgan` SELF / THS_PER / T / Y15-64 / geo=IT / citizen dims. Parser + dry-run implementati.

## 23. ISTAT mapping

P1 demografia (`OBS-IST-FOR-STOCK`); non pilot.

## 24. Regione Lombardia mapping

Territorio `ITC4` + CSV camerali OpenGov in allowlist; non dataset demografico sostitutivo UC.

## 25. OECD mapping

Complementare solo; non pilot.

## 26. Opportunity source mapping

incentivi.gov / EU F&T â†’ PENDING REVIEW â†’ publish. Campi in contratto Â§24â€“26. **No import ora.**

## 27. World Bank mapping

M1 link/resources; 5â€“8 candidati post-Osservatorio. **No import ora.**

## 28. Culture/event sources

Curation-first; CulturaItalia/compatibili solo con riuso+URL+date.

## 29. Editorial/press rule

Stampa = secondaria; risalire a fonte istituzionale.

## 30. Person import = NO

## 31. Business directory import = NO

(Aggregati Osservatorio sÃ¬.)

## 32. Acquisition methods

| Source | Method |
|---|---|
| Eurostat | API JSON-stat |
| Unioncamere OpenGov | CSV download allowlist |
| Futurae | SEMI curated file |
| Opportunity | API/portal + review |
| WB | API later (M1) |

## 33. Refresh frequencies

Annual sources; check quarterly for Eurostat updates; no cron in D1.2/D1.3A.

## 34. Reference period

`period_start` / `period_end` (anno civile).

## 35. retrieved_at

Sidecar only (P-D).

## 36. external IDs

Source: `external_identifier`. Observation: natural key string.

## 37. licenses

Per-source class + note; verify before ALLOW.

## 38. attribution

Required in UI/methodology for Eurostat + CC-BY camerali.

## 39. transformations

YoY/share = derived (`quality_code=derived`); source stock preserved separately.

## 40. idempotency

Natural key + checksum â†’ CREATE/UPDATE/UNCHANGED; supersede on revision.

## 41. schema validation

Eurostat parser fails loudly on missing dims / wrong label.

## 42. dry-run support

`dryRunLfsaEsgan` + `scripts/external-data/dry-run-eurostat-lfsa-esgan.mjs` â€” **dbWrites: 0**.

## 43. importer architecture

```
src/lib/external-data/
  types.ts
  checksum.ts
  natural-key.ts
  eurostat/lfsa-esgan.ts (+ tests)
scripts/external-data/
  dry-run-eurostat-lfsa-esgan.mjs
```

Fetch â†’ validate â†’ normalize â†’ map â†’ (dry-run report). Apply path **not enabled**.

## 44. sample fetches

Eurostat live 2026-08-13: FOR/IT/2023 = 287.7.

## 45. sample records

Normalized fixture in unit tests (4 cells NAT/FOR Ã— 2022/2023).

## 46. DB migration required?

**NO** for pilot (D1.2-A / P-D).

## 47. migration block

P-A deferred (`retrieved_at`, `external_observation_id`, `content_checksum`, `license_code`) â€” specificato in provenance contract; **non creato**.

## 48. first migration file

**N/A** (nessuna).

## 49. micro-review

N/A â€” no SQL unit.

## 50. apply = NO

## 51. Production import = NO

## 52. tests

`src/lib/external-data/eurostat/lfsa-esgan.test.ts` â€” parser, keys, checksum, idempotency, schema drift, provenance fields.

## 53. npm test

**PASS** â€” 196/196 (include suite D1.2 Eurostat lfsa_esgan).

## 54. typecheck

**PASS**

## 55. lint

**PASS** (0 errors; pre-existing unused import warning in terms test).

## 56. build

Not re-run in refresh pass after typecheck PASS on new modules; no Next route changes. Optional before D1.3.

## 57. docs

- `d1.2-data-mapping-ingestion-contract.md` (refresh baseline)
- `d1.2-observatory-pilot-mapping.md` **NEW**
- `d1.2-ingestion-validation-report.md` **NEW**
- existing catalog / provenance / roadmap / registry

## 58. files

Vedi Â§43 + Â§57. No migration SQL.

## 59. dirt preserved

YES â€” no `git add .`

## 60. git diff check

N/A â€” **commit = NO** this pass.

## 61. staging

NO

## 62. commit = NO

## 63. push = NO

## 64. blocking findings

None for D1.2 PASS.
Per D1.3 apply: Futurae curated file + Lombardia CCIAA allowlist.

## 65. important findings

- Remote `supabase migration list` auth failure this session â€” use filesystem head `20260818100000`.
- UNIQUE values senza `source_id` â†’ disciplina un code = una fonte.

## 66. non-blocking findings

CSV CCIAA eterogenei; Milano dataset dedicato puÃ² mancare; Opportunity UNIQUE external id solo convention.

## 67. human decisions required

1. Chi compila extract Futurae nazionale?
2. Allowlist CCIAA Lombardia v1 finale?
3. Auto-publish stats dopo dry-run OK?
4. Autorizzare avvio **D1.3A**?

## 68. first real ingestion recommended

**D1.3A:** dry-run Eurostat IT + UC Lombardia allowlist + Futurae IT stock/share/yoy â†’ apply **local** only after human GO â†’ never Production until validated.

## 69. readiness

| Gate | Status |
|---|---|
| Mapping/contract | READY |
| Dry-run Eurostat | READY |
| Migration | Not needed |
| Local real import | Authorizable in D1.3A |
| Production import | **NOT** authorizable yet |

## 70. Decisione finale

**D1.2 DATA MAPPING & INGESTION CONTRACT COMPLETATO â€” FONTI/LICENZE/PROVENANCE VERIFICATE â€” PILOTA OSSERVATORIO ITALIA+LOMBARDIA DEFINITO â€” IMPORTER/DRY-RUN PRONTI â€” NESSUNA MIGRATION NECESSARIA â€” PRIMO IMPORT REALE AUTORIZZABILE**

**STOP.**
NON apply migration.
NON Production import.
NON commit/push in questo pass (working tree lasciato per review umana).
NON avviare D1.3 senza GO esplicito.
