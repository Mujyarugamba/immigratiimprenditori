# D1.3A-1a â€” Observatory Production ingestion writer / grants review

**Date:** 2026-08-13
**Mode:** accelerata controllata
**Baseline git:** `65825e0` on `main` (ahead/behind `0/0`)
**Migration head (local=remote before this file):** `20260818100000`
**This task:** writer privilege gate only â€” **no apply, no Production ingestion, no commit/push**

---

## 1. Decision

`D1.3A-1a OBSERVATORY PRODUCTION WRITER REVIEW COMPLETATA â€” ROOT CAUSE CONFERMATA â€” LEAST-PRIVILEGE GRANTS/WRITER DETERMINATI â€” MIGRATION CREATA E MICRO-REVIEWATA â€” NESSUN APPLY ESEGUITO â€” NESSUNA INGESTION PRODUCTION ESEGUITA â€” LOCAL APPLY AUTORIZZABILE`

**STOP.** Next exact step (separate task): local apply of `20260819100000_grant_observatory_ingestion_writer.sql` only after GO; then Production credentials + remote migration apply; then Eurostat Production ingest for `OBS-EU-SELF-CIT` only.

---

## 2. Why Production was blocked (D1.3A)

Three independent gates (from D1.3A report):

1. **No Production Supabase credentials** in the workspace (`.env.local` â†’ local only).
2. **Committed schema** (`20260812280000_create_access_osservatorio_rls.sql`) grants Observatory table DML only to `authenticated` (plus `anon` SELECT) â€” **no explicit `service_role` SELECT/INSERT/UPDATE**. Local importer failed with `permission denied for table observatory_indicators` until an ops-only GRANT.
3. Unioncamere inputs missing (out of scope here).

Remote dump (2026-08-13) additionally shows hosted DB currently has **`GRANT ALL â€¦ TO service_role`** on the three Observatory tables (platform/default surface broader than least privilege). That is **excess**, not a substitute for a reviewed migration in-repo.

---

## 3. Importer auth path (real)

```
scripts/external-data/ingest-eurostat-lfsa-esgan.ts
  â†’ runEurostatIngest("apply")
  â†’ createIngestClient()
       NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
  â†’ @supabase/supabase-js createClient(url, serviceRoleKey)
  â†’ PostgREST with JWT role = service_role
  â†’ PostgreSQL role service_role (rolbypassrls = true)
  â†’ table privileges (SQL GRANT) on observatory_*
```

Not used for D1.3A apply: `anon`, end-user `authenticated`, direct Postgres URL, SECURITY DEFINER helper.

---

## 4. Tables / operations (importer)

| TABLE | OPERATION | WHY |
|---|---|---|
| `observatory_statistical_sources` | SELECT | find by `external_identifier` |
| | INSERT | create Eurostat source row |
| | UPDATE | refresh edition/license/methodology/url |
| `observatory_indicators` | SELECT | find by `code` |
| | INSERT | create unpublished indicator |
| | UPDATE | refresh texts; unpublish during import; publish after verify |
| `observatory_indicator_values` | SELECT | current logical row + post-write count |
| | INSERT | new observation / revised successor |
| | UPDATE | withdraw superseded row (`status=withdrawn`) |

**Not used:** DELETE, TRUNCATE, sequences, geographies tables, ON CONFLICT upsert.

**UPSERT semantics:** application-level SELECT â†’ INSERT **or** UPDATE(withdraw)+INSERT(revised). Requires SELECT+INSERT+UPDATE; **not** DELETE.

**Sequences:** PKs `uuid DEFAULT gen_random_uuid()` â€” **no sequence grants**.

---

## 5. Security matrix (pre-migration committed intent)

| ROLE | TABLE | SELECT | INSERT | UPDATE | DELETE | RLS RESULT |
|---|---|---|---|---|---|---|
| anon | indicators / sources / values | YES | NO | NO | NO | public read only (published/active filters) |
| authenticated (non-editor) | same | YES* | YES* | YES* | NO | *SQL grant present; RLS **blocks** write without `access_is_editor()` |
| authenticated (editor) | same | YES | YES | YES | NO | editorial write allowed (by design) |
| service_role (migrations) | same | **NO** | **NO** | **NO** | NO | BYPASSRLS irrelevant without table GRANT â†’ **permission denied** |

\* Table-level INSERT/UPDATE to `authenticated` is intentional for Redattori; ordinary users fail RLS (verified in harness).

**Post-migration intended `service_role`:** SELECT/INSERT/UPDATE only; DELETE denied; RLS unchanged (BYPASSRLS).

---

## 6. Solution chosen

**Preference #1 â€” minimal grants to existing server-side role `service_role`.**

Rejected for this gate:

- SECURITY DEFINER helper â€” unnecessary surface when BYPASSRLS + SIU grants suffice and match D1.2 â€œservice_role toolingâ€.
- Broad `GRANT ALL` / schema-wide grants â€” violates least privilege.
- New RLS policies for `service_role` â€” redundant with `rolbypassrls`.
- Changing editorial `authenticated` grants â€” out of scope; not required for ingestion.

Migration file:

`supabase/migrations/20260819100000_grant_observatory_ingestion_writer.sql`

Delta only:

```sql
REVOKE ALL â€¦ FROM service_role;  -- converge away from GRANT ALL / partial ACL
GRANT SELECT, INSERT, UPDATE â€¦ TO service_role;  -- importer-needed only
```

No RLS changes. No other domains. No provenance simplification.

---

## 7. Micro-review (adversarial)

| Class | Finding |
|---|---|
| BLOCKING | none after SIU-only design |
| IMPORTANT | Hosted dump currently shows `GRANT ALL` to `service_role` â€” migration **revokes** excess (incl. DELETE) |
| IMPORTANT | Production write still blocked until credentials + **migration apply** (not done here) |
| NON-BLOCKING | Local DB still carries D1.3A ops GRANT (incl. DELETE) until migration applied/reset â€” harness rolled back |
| NON-BLOCKING | Trigger functions still show dump `GRANT ALL` to anon/authenticated/service_role (pre-existing; not Observatory writer surface) |

Checks: least privilege; no anon write escalation; authenticated non-editor blocked by RLS; no DELETE for writer; no RLS weaken; BYPASSRLS understood; no SECURITY DEFINER; ownership remains `postgres`; importer idempotency compatible; public read policies untouched.

---

## 8. Transactional harness (ROLLBACK)

Executed against local Docker; **ROLLBACK** â€” leftover harness rows = 0; **no permanent migration apply**.

| Check | Result |
|---|---|
| service_role SIU | PASS |
| service_role DELETE | DENIED |
| anon write privileges | INSERT/UPDATE/DELETE = false |
| authenticated non-editor insert | blocked by RLS |
| UPSERT-like withdraw+revise | PASS |
| leftover after ROLLBACK | 0 |

---

## 9. What remains before Production write

1. Human GO + **local apply** of `20260819100000` (separate task).
2. Provide Production `SUPABASE_SERVICE_ROLE_KEY` / URL to ingest runtime (never commit).
3. **Remote apply** of the same migration.
4. Dry-run â†’ apply Eurostat `OBS-EU-SELF-CIT` only.
5. Unioncamere still BLOCKED (unchanged).

---

## 10. Out of scope (respected)

No D1.2 remap; no new indicators; no Unioncamere; no D1-B/C; no Production write; no remote/local permanent apply; no stage/commit/push.
